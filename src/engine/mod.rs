// =============================================================================
// fqc - Engine Module
// =============================================================================
//! Core compression engine: execution-mode routing (archive / streaming /
//! pipeline) and normalized compression requests.

pub mod compression_engine;
pub mod compression_request;
