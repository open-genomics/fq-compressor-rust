# Tasks

- [x] Introduce a stable benchmark dependency and register a small benchmark target set.
- [x] Add parser and archive workflow benchmarks that reuse generated data from existing test fixtures.
- [x] Verify `cargo bench --no-run` and the standard validation commands still pass.
