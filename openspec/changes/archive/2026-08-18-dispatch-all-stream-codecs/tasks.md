# Tasks: dispatch-all-stream-codecs

- [x] Strict codec parse helpers (`try_from_nibble`, `decode_codec_version`)
- [x] `decompress_raw` takes four codec IDs and dispatches each stream
- [x] Update tests/benches call sites
- [x] `tests/test_codec_dispatch.rs` for each stream + version + flag contradiction
- [x] CHANGELOG + OpenSpec artifacts
- [x] `cargo fmt/clippy/test/doc`
