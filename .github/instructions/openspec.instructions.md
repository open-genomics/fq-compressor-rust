# OpenSpec workflow

- `openspec/specs/` is the living source of truth.
- `openspec/changes/<change-id>/` holds change proposals, design notes, tasks, and capability deltas.
- Keep capability names stable and specific to this repository.
- Do not add generic process documents unless they clearly reduce maintenance cost for `fqc`.
